---
import bgShops from '../../assets/background/bg-shops.png';
const { title } = Astro.props;
---

<div
  class="h-[25rem] grid items-center bg-top bg-cover"
  style={`background-image: url(${bgShops.src});`}
>
  <h2 class="text-center text-7xl font-bold text-white">{title}</h2>
</div>
