---
interface Props {
  variant: 'primary' | 'secondary' | 'white';
  href: string;
  target?: '_blank' | '_self';
  class?: string;
}

const {
  variant = 'primary',
  href,
  class: className,
  target = '_self',
} = Astro.props;
let buttonClass = className;

const variantClasses: Record<string, string> = {
  primary:
    'bg-primary border-primary text-white hover:bg-transparent hover:text-primary',
  secondary:
    'bg-secondary border-secondary text-white hover:bg-transparent hover:text-secondary', // TODO: Change this if necessary
  white:
    'bg-white border-white text-primary hover:bg-transparent hover:text-white', // TODO: Change this if necessary
};

if (variantClasses[variant]) {
  buttonClass += ` ${variantClasses[variant]}`;
}
---

<a
  href={href}
  target={target}
  class:list={buttonClass}
  class='cursor-pointer uppercase transition-colors border-2 bg-primary py-2 px-6 rounded-xl font-semibold no-underline focus:outline-none inline-block'
>
  <slot />
</a>
